spring:
  application:
    name: discovery
  profiles:
    active: dev
  cloud:
    config:
      uri: http://192.168.1.7:8888
      enabled: true
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false