spring:
  application:
    name: doc-delivery
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true
    discovery:
      client:
        health-indicator:
          enabled: true



